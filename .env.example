# Django settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database (PostgreSQL)
DATABASE_URL=postgres://peterelmwood:peterelmwood@db:5432/peterelmwood_db

# GCP Storage (production only)
GS_BUCKET_NAME=your-bucket-name
GS_CREDENTIALS_FILE=/secrets/gcp-credentials.json

# Email settings (production only)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# HTTPS settings (production only)
SECURE_SSL_REDIRECT=False
